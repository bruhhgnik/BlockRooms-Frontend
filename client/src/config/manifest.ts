// Stub manifest for Solana version (replaces Starknet manifests)

export const manifest = {
  world: {
    address: "0x0", // Placeholder for Solana program ID
    name: "BlockRooms",
  },
};

export type Manifest = typeof manifest;
